import assert from 'node:assert/strict';
import {commandEngineLimits, executeCommand} from '../src/lib/command-engine.mjs';
assert.deepEqual(commandEngineLimits, {maxCommandLength:240,maxCommandsPerMinute:20,maxTokens:32,maxTokenLength:160,maxFlags:16});
const safe=['help','version','contracts','exports','doctor','doctor --json','config check --preset production','origin check https://panel.example.com','session policy --ttl 86400000 --idle 1800000 --rotate 900000 --max 5','store check --session persistent --oauth persistent --rate redis --lock redis --audit persistent','store list','store contract','idempotency reservation','transaction finality','deadline settlement','action test guild.settings.update --method PATCH --origin https://panel.example.com --session valid --csrf valid --permission MANAGE_GUILD --bot-permission VIEW_CHANNEL','rate-limit test --requests 8 --limit 5','realtime check --transport websocket --sequence 42 --resume 40','realtime limits','realtime transports','realtime replay','cli doctor','cli writing-check','cli migration','security explain csrf','examples'];
for (const locale of ['en','tr']) for (const command of safe) {const result=executeCommand(command,{locale});assert.equal(result.ok,true,`${locale}: ${command}`);assert.ok(result.lines.length);}
for (const unsafe of ['doctor && whoami','doctor; rm -rf','first\nsecond','ghp_' + 'abcdefghijklmnopqrstuvwxyz123456','Bearer abcdefghijklmnopqrstuvwxyz123456','__proto__','doctor --constructor x']) assert.equal(executeCommand(unsafe,{locale:'en'}).ok,false,unsafe);
assert.equal(executeCommand('clear',{locale:'tr'}).clear,true);
console.log('Command engine passed: bilingual allowlist, parsing limits, secret guard and safe output.');
