---
title: action() işlem tanımı
description: Korunan yönetim paneli işlemlerinin tek tanımda kurulması.
---

import CommandPlayground from '@site/src/components/CommandPlayground';

# `action()` işlem tanımı

`gate.action()` bir yazma işleminin giriş, kaynak, sınır, yetki ve çalıştırma adımlarını aynı tanımda gösterir. Kontrollerin sırası uç nokta dosyalarına dağılmaz.

```ts
const updateSettings = gate.action({
  name: "guild.settings.update",
  parse(value) {
    const body = parseBody(value);
    return {
      guildId: body.guildId,
      revision: body.revision,
      settings: body.settings,
    };
  },
  resource: (input) => ({
    type: "guild",
    id: input.guildId,
  }),
  rateLimit: {
    limit: 20,
    windowMs: 60_000,
    key: (context, input) => `${context.userId}:${input.guildId}`,
  },
  idempotency: {
    ttlMs: 10 * 60_000,
    scope: (context, input) => `${context.userId}:${input.guildId}`,
  },
  concurrency: {
    key: (_context, input) => `guild-settings:${input.guildId}`,
    ttlMs: 8_000,
    waitMs: 500,
  },
  timeoutMs: 5_000,
  authorize: async (context, input) => {
    return canManageGuild(context.userId, input.guildId);
  },
  execute: async (context, input) => {
    return saveGuildSettings(context, input);
  },
});
```

## Adımlar

`parse` ham girdiyi doğrular ve gerekli alanlara indirger. `resource` hedef kaynağı tanımlar. `authorize` kullanıcının bu kaynak üzerinde işlem yapıp yapamayacağını belirler. `execute`, önceki kontroller geçerse çağrılır.

## Tekrarlı istek koruması

İstemci ağ sorunu nedeniyle aynı yazma isteğini yeniden gönderebilir. Aynı tekrarlı istek anahtarı ikinci kez kayıt üretmemelidir. Kapsamı kullanıcı ve kaynakla sınırlandırmak, farklı kullanıcıların aynı anahtarı paylaşmasını engeller.

## Kaynak kilidi

`concurrency.key`, aynı sunucu ayarına eşzamanlı yazmaları sıraya alır veya reddeder. Bu kontrol revision denetiminin yerine geçmez. Kilit eşzamanlı yazmayı, revision ise eski veriyle güncelleme yapılmasını ele alır.

## İşlem sırasını dene

Aşağıdaki terminal gerçek uç noktaya bağlanmaz. `action test` komutunu izin listesi üzerinde çalıştırır.

<CommandPlayground compact />
